close all
clear all


regexFile = 'data/acc*.mat';
files = dir(regexFile);


gyro = [];

for k=1:length(files)
    file = files(k);
    name = file.name;
    path = file.folder;
    fileNamePath = [path '/' name];
    
    disp(['Load experiment: ' name]);
    
    temp = load(fileNamePath);
    expt(k) = temp.expt;
    
    gyro = [gyro, expt(k).imu.gyro];
end

%??????????????????????????????????????????????????????????????????????????
%   Compute the mean of the gyro measurements in gyro. gyro is 3 x N.
w = zeros(3,1);
%??????????????????????????????????????????????????????????????????????????

disp('Offset:');
disp('w =');
disp(w);

Tw_raw_gyro = eye(4);

%??????????????????????????????????????????????????????????????????????????
%   Compose the homogeneous calibration matrix Tw_raw_gyro for the 
%   gyroscope. This matrix only contains the offsets.


%??????????????????????????????????????????????????????????????????????????

save('Tw_raw_gyro.mat','Tw_raw_gyro');



